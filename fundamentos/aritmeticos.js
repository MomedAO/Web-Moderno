const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;
const subtracao = d - b;
const muliplicacao = a * b;
const divisao = d / a;
const resto = a % 2;

console.log(soma, subtracao, muliplicacao, divisao, resto);

const [e, f, g, h] = [4, 55, 26, 7];

const som = e + f + g + h;
const sub = f - g;
const mult = e * f;
const div = g / e;
const res = h % 2;

console.log(som, sub, mult, div, res);