const raio = 2;
const areaDaCircunferencia = Math.PI * Math.pow(raio, 2);

console.log('O resultado da area = ' + areaDaCircunferencia + 'm2');

const r = 6;
const areaCircun = Math.PI * Math.pow(r, 2)
console.log(`Area e' ${areaCircun} m2`);